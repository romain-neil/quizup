{% extends 'base.html.twig' %}

{% block title %}Ajouter une question{% endblock %}

{% block body %}
<div>
	<p><a href="{{ path('admin_show_questions') }}">Retour à l'administeration</a></p>
	{{ form_start(form) }}
		{{ form_row(form.libele) }}

		{{ form_row(form.image) }}

		{{ form_rest(form) }}

		<input type="submit" value="Créer la question" />
	{{ form_end(form) }}
</div>
{% endblock %}
